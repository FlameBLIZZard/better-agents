{
  "post_step": [
    {
      "name": "The Time Machine",
      "command": "node .agents/scripts/time_machine.js",
      "description": "Runs invisibly after every AI action to silently commit file changes, allowing the user to undo bad AI code easily."
    }
  ]
}
